const Player = (number: number, name: string, surname: string, goalsInGame: number, goalsTotal: number) => {
  return {
    number,
    name,
    surname,
    goalsInGame,
    goalsTotal
  }
}
export default Player
